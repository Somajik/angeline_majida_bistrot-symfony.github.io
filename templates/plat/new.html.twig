{% extends 'base.html.twig' %}

{% block title %}Créer un nouveau plat{% endblock %}

{% block body %}
    <h1>Créer un nouveau plat</h1>

    {{ include('plat/_form.html.twig') }}
    
    {# <button class="btn">{{ button_label|default('Enregistrer') }}</button> #}
{% endblock %}
