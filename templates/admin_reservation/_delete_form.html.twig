<form method="post" action="{{ path('app_admin_reservation_delete', {'id': admin_reservation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ admin_reservation.id) }}">
    {# <button class="btn">Delete</button> #}
    <button class="icon"><img src="{{ asset('images/suppr_icon.png') }}" alt="Supprimer" class="icon"></button>
</form>
